language: cpp
compiler:
  - clang
  - gcc
branches:
  - all
before_script:
  - sudo apt-get install libleveldb-dev swig
script:
  cd monad-c && mkdir build && cd build && cmake -DCMAKE_BUILD_TYPE=Release .. && make
os:
  - linux
  - osx
osx_image: xcode61
